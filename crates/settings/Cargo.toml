[package]
name = "wkhtmltopdf-settings"
version = "0.1.0"
edition = "2021"
description = "Configuration and settings structs for wkhtmltopdf"

[dependencies]
serde = { version = "1", features = ["derive"] }
